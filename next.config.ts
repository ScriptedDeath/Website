import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: false,  // Disable React Strict Mode
  // Other config options here, such as images, i18n, etc.
};

export default nextConfig;
